# DreamCoder V1 Algorithmic Audit & Stress Test

**结论：核心算法机制通过检查，但当前 V1 尚未通过“复杂新组合上稳定、互补提升”的严格整体算法验证。** Recognition 的任务条件化有效；库具有有限迁移证据，但压力测试没有出现普遍的 `D > B,C > A`，深度 5–8 全部失败。本报告不将原先 100% 的 toy 结果视为泛化证明。

复现：`python -m pytest -q`，然后 `python run_audit.py`。22 项测试通过，包括原有 8 项。正式数据位于 [results/audit/summary.json](results/audit/summary.json)；逐任务 frontier 和每次 shuffle 的完整指标在同目录的 `original.json`、`transfer.json`、`stress.json`。初次诊断数据保留在 `results/audit_initial/`，不与正式结果混用。

## 1. 实现审计及修复

| 检查 | 发现与处理 |
|---|---|
| Grammar / Wake | 真正按负 log prior 与可采纳的类型完成下界枚举；改变 prior 会改变顺序。新增独立穷举 oracle，验证含分支 AST 的全集、代价顺序和 top-K，而非只检验同一实现内部结果。 |
| Frontier | 保存至多 K 个不同 AST，全部满足所有 I/O，后验按 prior 排序；同分截断允许任选。预算耗尽时可不足 K。不是只保留单解。 |
| Library learning | 从 36 个真实 MAP 解中发现重复子树 `rotate90(trim($input))`，注册为 `learned_0 : Grid → Grid`，不是事先植入宏。基础词表保持 24，新词表为 25（18 个操作、6 个常量、1 个学习宏）。 |
| MDL | 原始 108→100 正确：36×3=108；12 次出现替换为大小 2 的宏调用，语料变为 96；定义大小 3 加注册开销 1，总计 100。它是 AST token MDL，不是压缩字节或负 log prior。修复了后续压缩调用未计入已有库定义成本的问题。 |
| Recognition | MLP 从实际 I/O 特征输出每个任务的 primitive logits，混合基础 prior 后直接传给枚举器。新测试确认改变任务名或 ground truth 不影响特征、grammar 或搜索结果；审计中的模型输入另行去除元数据。 |
| Dream / Replay | Dream 确实先从 grammar 采样再执行产生 I/O；600 个 fantasy pairs 全部可执行。Replay 来自 108 个真实 Wake frontier 解，逐个复核满足原任务。修复 Dream 在最后一次允许尝试恰好生成足量样本却错误抛异常的问题。 |
| 原 train/test | 输入和完整 I/O 无重复，但 **24/24 测试任务的完整程序均在训练中出现**，canonical 与 probe 行为也全部重合。不是直接读取测试答案的实现泄漏，但作为组合泛化评估存在完全的程序/模板重叠。 |
| A baseline | 24 个基础 primitive 的 log probability 全为 `-log(24)`；压缩与训练前后保持不变，没有被新库或 recognition 污染。 |
| B 组归因 | B 同时使用新库和计数学习 prior；原 B/A 不能证明收益全部来自抽象。新增 `base_fitted` 和 `library_uniform` 两项控制。 |
| Recovery 指标 | 原 exact 实际为宏展开 AST recovery，已拆开；原首解均值将未解任务的预算节点当成首解，已修复。新增原始/展开/canonical/行为恢复、已解样本数、独立截尾指标。 |
| 复杂度与预算 | 初次诊断发现重复 solid、旋转与 border 交换会虚增深度。新增仅用于评估的语义保持化简，随后重新生成并分桶；保留初次结果。新增明确停止原因和正预算校验，分别统计 top-K 未满的耗尽与完全未解的耗尽。 |

核心实现没有加入 ARC-3、RL、LLM，没有改造搜索器、网络架构或训练目标。概率仍是原有 primitive 乘积分数；Dream 采用类型/深度约束后的局部归一化采样，不是对无限 AST prior 的精确无条件采样。

## 2. 固定实验设置与隔离

- 沿用原 V1：seed=7，36 个 Wake 训练任务，600 dreams，108 replay pairs 重采样 5 倍，MLP 100 epochs，单线程确定性 CPU。C/D 见到相同任务语义，C 将标签中的宏展开。
- 模型训练完成并冻结后才生成测试集。测试选择只依据类型、复杂度、数据重合和退化行为，**不依据任何配置的求解结果**；未筛选获胜 seed 或调整训练超参数。
- 48 个 stress 任务，化简后应用深度 1–2、3–4、5–6、7–8 各 12 个。实际深度 1…8 数量为 3/9/5/7/5/7/10/2，同时保存 AST size；深度不是已证明的最短程序长度。
- 另有 12 个 library transfer 任务，`g(f(x))` 与 `f(h(x))` 各 6 个，f 为真实学到的宏。包含颜色、平移、边框等新组合。
- 两个新组合集合统一 `top_k=3, max_nodes=600, max_states=6000, max_size=33`。全部 ground truth 在 size 限制内；A/B/C/D 和 shuffle 的预算完全相同。原任务 sanity check 沿用 1500 nodes、100000 states、size≤7。
- 每个新任务 6 对 I/O。对 **Wake ground truth、所有 Replay 解及所有 Dream 标签** 做程序隔离；两组新测试的 canonical 重合、32 个独立筛选 probe 上的行为重合、训练输入重合、完整任务 I/O 重合均为 **0**。共享 primitives 与学到的子程序是预期迁移，不算泄漏。
- 恢复评估另外使用 64 个独立 probe，包含稀疏网格、全背景和尺寸边界；不复用筛选 probe。有限 probe 不能证明完整语义等价，也不能穷尽未知的等价程序。
- C、D 各做 3 次固定 seed 的 derangement shuffle：没有任务拿到自己的 grammar，保留预测 grammar 的整体分布。下面 shuffle 行是三次重复的描述性汇总，不能当成三倍独立任务。

完整训练与测试数据、shuffle 映射、模型参数、prior、MDL 明细及 benchmark 哈希均已保存。修正复杂度前后的两次完整训练，训练数据和两套模型张量逐项完全相同，见 `validation.json`。

## 3. 原任务 sanity checks

24 个任务的所有配置均 100% solved / behavioral recovery；以下为收集 top-3 的平均候选程序数：

| A | B | C | D | C shuffle | D shuffle | Base + fitted prior | Library + uniform prior |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 131.33 | 19.67 | 12.04 | 10.96 | 142.24 | 83.94 | 16.00 | 105.67 |

Recognition 的 conditioning 确实起作用。三次 C shuffle 节点分别为 157.33、109.83、159.54；与正确 C 的配对 bootstrap 节点节省 95% 区间分别为 [96.71,194.92]、[46.96,161.04]、[98.50,195.67]，均排除 0。D 同样如此。

库单独的速度收益存在但较小：uniform 新库使 131.33→105.67；**只有 fitted prior 就达到 16.00，甚至优于 B 的 19.67**。因此原 B 的大部分加速不能归因于新抽象。原 B/D 的 raw exact 仅 66.7%，宏展开/canonical/behavioral 均为 100%，表明语法差异不能被误判为合成失败。

## 4. 正式 stress 结果

恢复率分母均为全部 48 个任务；“展开”指宏展开 AST，“canonical”指本审计实现的、经测试验证的等价化简，不是完整等价判定器。

| 配置 | Solve | Raw exact | 展开 AST | Canonical | Behavioral |
|---|---:|---:|---:|---:|---:|
| A | 12.50% (6) | 12.50% | 12.50% | 12.50% | 12.50% |
| B | 4.17% (2) | 2.08% | 2.08% | 4.17% | 4.17% |
| C | 18.75% (9) | 14.58% | 14.58% | 18.75% | 18.75% |
| D | 18.75% (9) | 14.58% | 14.58% | 18.75% | 18.75% |
| C shuffle | 10.42% | 8.33% | 8.33% | 10.42% | 10.42% |
| D shuffle | 9.03% | 7.64% | 7.64% | 9.03% | 9.03% |
| Base + fitted prior | 6.25% (3) | 4.17% | 4.17% | 6.25% | 6.25% |
| Library + uniform prior | 10.42% (5) | 10.42% | 10.42% | 10.42% | 10.42% |

| 配置 | 平均枚举节点 | 中位节点 | 平均展开状态 | 平均首解节点¹ | 平均搜索 ms | 耗尽率² |
|---|---:|---:|---:|---:|---:|---:|
| A | 567.65 | 600 | 1401.44 | 161.67 | 240.9 | 93.75% |
| B | 600.00 | 600 | 1228.00 | 119.50 | 258.9 | 100.00% |
| C | 539.27 | 600 | 1085.25 | 32.89 | 199.5 | 83.33% |
| D | 551.38 | 600 | 1107.88 | 62.00 | 222.0 | 87.50% |
| C shuffle | 564.33 | 600 | 1137.63 | 136.93 | 206.5 | 91.67% |
| D shuffle | 575.09 | 600 | 1157.99 | 120.69 | 235.1 | 93.06% |
| Base + fitted prior | 591.69 | 600 | 1294.60 | 133.00 | 286.7 | 97.92% |
| Library + uniform prior | 568.38 | 600 | 1685.71 | 93.80 | 352.1 | 93.75% |

¹ 首解均值仅包括各配置已解任务，分母不同，不能单独用于配置排名。JSON 同时保存样本数和截尾指标。² 已找到一两个解但尚未填满 frontier，也可能耗尽预算。此处搜索时间不含 recognition；C/D 的平均总时间分别为 200.8/223.5 ms，JSON 独立保存特征与推理耗时。

| 化简后深度 | A solved | B solved | C solved | D solved | C shuffle | D shuffle |
|---|---:|---:|---:|---:|---:|---:|
| 1–2 | 4/12 | 2/12 | 8/12 | 8/12 | 36.11% | 33.33% |
| 3–4 | 2/12 | 0/12 | 1/12 | 1/12 | 5.56% | 2.78% |
| 5–6 | 0/12 | 0/12 | 0/12 | 0/12 | 0% | 0% |
| 7–8 | 0/12 | 0/12 | 0/12 | 0/12 | 0% | 0% |

深度 5–8 所有配置均 100% 耗尽。每个 bucket 的全部恢复、节点、时间和预算指标均在 JSON，而非仅保存表中的 solve rate。

D 相对 A 有 4 个新增成功、1 个反向失败，配对单侧检验 p=0.1875，不能宣称稳健提升。C 和 D 解出的任务完全相同；D 反而多用平均 12.10 个节点（配对 bootstrap 95% CI [2.92,24.06]）。三次 shuffle 的 solve 差异也并非均显著，不能挑选其中一个 p 值充当整体证据。所有区间仅针对这一冻结模型和测试集，未覆盖训练 seed 的不确定性。

## 5. Library transfer

| 配置 | Solve / Behavioral | 平均节点 | 平均首解节点 | 耗尽率 |
|---|---:|---:|---:|---:|
| A | 0/12 | 600.00 | — | 100% |
| B | 0/12 | 600.00 | — | 100% |
| C | 2/12 | 600.00 | 416.00 | 100% |
| D | 3/12 | 564.42 | 149.67 | 83.33% |
| C shuffle | 5.56% | 598.25 | 325.50 | 97.22% |
| D shuffle | 11.11% | 590.81 | 179.25 | 97.22% |

D 的三个成功程序均实际使用新宏，并通过独立行为 probe，例如：

```text
f(x) = rotate90(trim(x))
solid(f(x), red)                         # frontier 也发现 f(solid(x, red))
translate(f(x), one, one)
translate(f(x), minus_one, minus_one)
```

这些完整组合在所有训练程序及训练行为指纹中均未出现。D 在 `g(f(x))` 子组为 3/6，`f(h(x))` 子组为 0/6；C 分别为 2/6、0/6。后者保留的是去除与前者等价重复后的独特任务，不能把可交换的等价程序当成第二份独立成功。

D 的 raw exact=0、展开/canonical/behavioral=25%，因为搜索使用宏；这不是三次 synthesis failure。D 比 C 仅多解一个任务，配对单侧 p=0.5，属于有限互补性示例，尚无稳定统计证据。

## 6. 最终判断

1. **Library 是否降低搜索复杂度？** 在原分布上有可隔离的局部速度收益及正确 MDL 降低；更广的新组合上没有稳定收益，B 甚至劣于 A。
2. **Recognition 是否真正 task-conditioned？** 是，原任务的 shuffle 对照明确支持；新组合上平均更好，但优势随任务和 shuffle 变化，不能外推为普适能力。
3. **Library 与 recognition 是否互补？** 有一个额外迁移成功的局部证据；整体 stress 中 D=C 且更慢，严格的稳定互补性未通过。
4. **抽象能否迁移到未见组合？** 能迁移到三个 `g(f(x))` 实例；独特 `f(h(x))` 子组全部失败，双向组合迁移未通过。
5. **固定预算能否解决 base 失败的复杂任务？** D 解出 A 全部失败的三个深度 3 迁移实例，另在 stress 有 4 个 A 未解任务；但最高深度段全部失败，没有深层突破。
6. **是否存在 leakage / artifact？** 未发现代码读取测试 ground truth 的直接泄漏；原程序模板完全重叠、prior 归因混杂、exact 与首解指标定义存在问题，均已修复或加入独立控制。领域手工特征明显偏向原任务，且此审计只保留原三程序族 Wake 训练和原 Dream 分布；新 benchmark 是有意构造的组合分布外评估。

因此：**机制级验证通过；严格的整体 algorithmic validation 尚未通过。** 后续若要声称一般性 DreamCoder 优势，应在更多训练程序族、更多独立 seed 与相同程序隔离约束下复核，而不是继续引用原 100% toy 指标。本轮没有为改善结果新增模型功能或调参。
